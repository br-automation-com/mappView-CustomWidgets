define(['brease/enum/Enum'], function (Enum) {

    'use strict';

    /**
     * @class widgets.breaseXtended.NumericInput.Config
     * @extends core.javascript.Object
     * @override widgets.breaseXtended.NumericInput
     */

    var config = {
    };

    return config;
});
